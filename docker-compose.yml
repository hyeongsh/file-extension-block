services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: feb-app
    expose:
      - "8080"
    ports:
      - "8080:8080"
    restart: unless-stopped

#  nginx:
#    image: nginx:1.27-alpine
#    container_name: feb-nginx
#    depends_on:
#      - app
#    ports:
#      - "80:80"
#      - "443:443"
#    volumes:
#      - ./nginx.conf:/etc/nginx/nginx.conf:ro
#    restart: unless-stopped

